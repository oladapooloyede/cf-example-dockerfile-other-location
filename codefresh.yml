version: '1.0'

steps:

  build_prj:
    type: build
    description: Build an Image by Specifying the Dockerfile Location
    image_name: codefreshio/yaml-example-specify-location
    dockerfile: docker/Dockerfile
    tag: ${{CF_BRANCH}}